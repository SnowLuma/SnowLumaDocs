---
title: "get_stranger_info"
---

import { ActionHeader } from '../../../components/ActionHeader';
import { Tabs, Tab } from '@theme';

<ActionHeader name={"get_stranger_info"} summary={"获取陌生人信息"} readOnly={true} category={"好友"} />

Call via `POST http://127.0.0.1:3000/get_stranger_info` (JSON params as the body) or WebSocket.

## Parameters

| Param | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `user_id` | uint | ✓ |  | QQ 号 |

## Returns (data)

陌生人资料：QQ 号、昵称、性别、年龄，命中资料时另含等级。

Every response is wrapped in the standard envelope: `{ "status": "ok", "retcode": 0, "data": ... }`. The table below describes `data`.

| Field | Type | Required | Description |
| --- | --- | --- | --- |
| `user_id` | integer | ✓ | QQ 号 |
| `nickname` | string | ✓ | 昵称 |
| `sex` | string | ✓ | 性别（male/female/unknown） |
| `age` | integer | ✓ | 年龄 |
| `qq_level` | integer | – | QQ 等级（仅查到资料时返回） |
| `level` | integer | – | QQ 等级，同 qq_level（仅查到资料时返回） |

<details>
<summary>Raw JSON Schema</summary>

```json
{
  "type": "object",
  "properties": {
    "user_id": {
      "type": "integer",
      "description": "QQ 号"
    },
    "nickname": {
      "type": "string",
      "description": "昵称"
    },
    "sex": {
      "type": "string",
      "description": "性别（male/female/unknown）"
    },
    "age": {
      "type": "integer",
      "description": "年龄"
    },
    "qq_level": {
      "type": "integer",
      "description": "QQ 等级（仅查到资料时返回）"
    },
    "level": {
      "type": "integer",
      "description": "QQ 等级，同 qq_level（仅查到资料时返回）"
    }
  },
  "required": [
    "user_id",
    "nickname",
    "sex",
    "age"
  ]
}
```

</details>

## Examples

<Tabs>
<Tab label="curl">

```bash
curl -X POST http://127.0.0.1:3000/get_stranger_info \
  -H 'Content-Type: application/json' \
  -H 'Authorization: Bearer <access-token>' \
  -d '{"user_id":10001}'
```

</Tab>
<Tab label="Python">

```python
import requests

resp = requests.post(
    "http://127.0.0.1:3000/get_stranger_info",
    json={
        "user_id": 10001
    },
    headers={"Authorization": "Bearer <access-token>"},  # if an access token is configured
)
print(resp.json())
```

</Tab>
<Tab label="JavaScript">

```js
const resp = await fetch("http://127.0.0.1:3000/get_stranger_info", {
  method: "POST",
  headers: {
    "Content-Type": "application/json",
    "Authorization": "Bearer <access-token>", // if an access token is configured
  },
  body: JSON.stringify({
  "user_id": 10001
}),
});
console.log(await resp.json());
```

</Tab>
<Tab label="Go">

```go
package main

import (
	"bytes"
	"fmt"
	"io"
	"net/http"
)

func main() {
	body := []byte(`{"user_id":10001}`)
	req, _ := http.NewRequest("POST", "http://127.0.0.1:3000/get_stranger_info", bytes.NewReader(body))
	req.Header.Set("Content-Type", "application/json")
	req.Header.Set("Authorization", "Bearer <access-token>") // if an access token is configured
	resp, err := http.DefaultClient.Do(req)
	if err != nil {
		panic(err)
	}
	defer resp.Body.Close()
	out, _ := io.ReadAll(resp.Body)
	fmt.Println(string(out))
}
```

</Tab>
<Tab label="SnowLuma SDK">

```ts
import { SnowLumaHttpClient } from '@snowluma/sdk';

const bot = new SnowLumaHttpClient({
  baseUrl: 'http://127.0.0.1:3000/',
  accessToken: process.env.SNOWLUMA_TOKEN, // if an access token is configured
});

const data = await bot.call('get_stranger_info', {
  "user_id": 10001
});
console.log(data);
```

</Tab>
</Tabs>

:::tip Using an AI assistant?
MCP-capable clients can discover and call this action directly — no hand-written HTTP. See [MCP](/en/mcp/).
:::
