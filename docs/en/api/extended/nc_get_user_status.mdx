---
title: "nc_get_user_status"
---

import { ActionHeader } from '../../../components/ActionHeader';
import { Tabs, Tab } from '@theme';

<ActionHeader name={"nc_get_user_status"} summary={"获取用户在线/扩展状态"} readOnly={true} category={"扩展"} />

Call via `POST http://127.0.0.1:3000/nc_get_user_status` (JSON params as the body) or WebSocket.

## Parameters

| Param | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `user_id` | uint | ✓ |  |  |

## Returns (data)

\{ status, ext_status \}：用户在线状态码与扩展状态码。

Every response is wrapped in the standard envelope: `{ "status": "ok", "retcode": 0, "data": ... }`. The table below describes `data`.

| Field | Type | Required | Description |
| --- | --- | --- | --- |
| `status` | integer | ✓ | 在线状态码 |
| `ext_status` | integer | ✓ | 扩展状态码 |

<details>
<summary>Raw JSON Schema</summary>

```json
{
  "type": "object",
  "properties": {
    "status": {
      "type": "integer",
      "description": "在线状态码"
    },
    "ext_status": {
      "type": "integer",
      "description": "扩展状态码"
    }
  },
  "required": [
    "status",
    "ext_status"
  ]
}
```

</details>

## Examples

<Tabs>
<Tab label="curl">

```bash
curl -X POST http://127.0.0.1:3000/nc_get_user_status \
  -H 'Content-Type: application/json' \
  -H 'Authorization: Bearer <access-token>' \
  -d '{"user_id":10001}'
```

</Tab>
<Tab label="Python">

```python
import requests

resp = requests.post(
    "http://127.0.0.1:3000/nc_get_user_status",
    json={
        "user_id": 10001
    },
    headers={"Authorization": "Bearer <access-token>"},  # if an access token is configured
)
print(resp.json())
```

</Tab>
<Tab label="JavaScript">

```js
const resp = await fetch("http://127.0.0.1:3000/nc_get_user_status", {
  method: "POST",
  headers: {
    "Content-Type": "application/json",
    "Authorization": "Bearer <access-token>", // if an access token is configured
  },
  body: JSON.stringify({
  "user_id": 10001
}),
});
console.log(await resp.json());
```

</Tab>
<Tab label="Go">

```go
package main

import (
	"bytes"
	"fmt"
	"io"
	"net/http"
)

func main() {
	body := []byte(`{"user_id":10001}`)
	req, _ := http.NewRequest("POST", "http://127.0.0.1:3000/nc_get_user_status", bytes.NewReader(body))
	req.Header.Set("Content-Type", "application/json")
	req.Header.Set("Authorization", "Bearer <access-token>") // if an access token is configured
	resp, err := http.DefaultClient.Do(req)
	if err != nil {
		panic(err)
	}
	defer resp.Body.Close()
	out, _ := io.ReadAll(resp.Body)
	fmt.Println(string(out))
}
```

</Tab>
<Tab label="SnowLuma SDK">

```ts
import { SnowLumaHttpClient } from '@snowluma/sdk';

const bot = new SnowLumaHttpClient({
  baseUrl: 'http://127.0.0.1:3000/',
  accessToken: process.env.SNOWLUMA_TOKEN, // if an access token is configured
});

const data = await bot.call('nc_get_user_status', {
  "user_id": 10001
});
console.log(data);
```

</Tab>
</Tabs>

:::tip Using an AI assistant?
MCP-capable clients can discover and call this action directly — no hand-written HTTP. See [MCP](/en/mcp/).
:::
