---
title: "delete_custom_face"
---

import { ActionHeader } from '../../../components/ActionHeader';
import { Tabs, Tab } from '@theme';

<ActionHeader name={"delete_custom_face"} summary={"删除收藏表情"} readOnly={false} category={"扩展"} />

Call via `POST http://127.0.0.1:3000/delete_custom_face` (JSON params as the body) or WebSocket.

## Parameters

| Param | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `emoji_id` | string | ✓ |  |  |

## Returns (data)

Returns the standard OneBot envelope; the `data` shape is not yet documented.

## Examples

<Tabs>
<Tab label="curl">

```bash
curl -X POST http://127.0.0.1:3000/delete_custom_face \
  -H 'Content-Type: application/json' \
  -H 'Authorization: Bearer <access-token>' \
  -d '{"emoji_id":""}'
```

</Tab>
<Tab label="Python">

```python
import requests

resp = requests.post(
    "http://127.0.0.1:3000/delete_custom_face",
    json={
        "emoji_id": ""
    },
    headers={"Authorization": "Bearer <access-token>"},  # if an access token is configured
)
print(resp.json())
```

</Tab>
<Tab label="JavaScript">

```js
const resp = await fetch("http://127.0.0.1:3000/delete_custom_face", {
  method: "POST",
  headers: {
    "Content-Type": "application/json",
    "Authorization": "Bearer <access-token>", // if an access token is configured
  },
  body: JSON.stringify({
  "emoji_id": ""
}),
});
console.log(await resp.json());
```

</Tab>
<Tab label="Go">

```go
package main

import (
	"bytes"
	"fmt"
	"io"
	"net/http"
)

func main() {
	body := []byte(`{"emoji_id":""}`)
	req, _ := http.NewRequest("POST", "http://127.0.0.1:3000/delete_custom_face", bytes.NewReader(body))
	req.Header.Set("Content-Type", "application/json")
	req.Header.Set("Authorization", "Bearer <access-token>") // if an access token is configured
	resp, err := http.DefaultClient.Do(req)
	if err != nil {
		panic(err)
	}
	defer resp.Body.Close()
	out, _ := io.ReadAll(resp.Body)
	fmt.Println(string(out))
}
```

</Tab>
<Tab label="SnowLuma SDK">

```ts
import { SnowLumaHttpClient } from '@snowluma/sdk';

const bot = new SnowLumaHttpClient({
  baseUrl: 'http://127.0.0.1:3000/',
  accessToken: process.env.SNOWLUMA_TOKEN, // if an access token is configured
});

const data = await bot.call('delete_custom_face', {
  "emoji_id": ""
});
console.log(data);
```

</Tab>
</Tabs>

:::tip Using an AI assistant?
MCP-capable clients can discover and call this action directly — no hand-written HTTP. See [MCP](/en/mcp/).
:::
