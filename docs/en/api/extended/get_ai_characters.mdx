---
title: "get_ai_characters"
---

import { ActionHeader } from '../../../components/ActionHeader';
import { Tabs, Tab } from '@theme';

<ActionHeader name={"get_ai_characters"} summary={"获取 AI 语音角色"} readOnly={true} category={"扩展"} />

Call via `POST http://127.0.0.1:3000/get_ai_characters` (JSON params as the body) or WebSocket.

## Parameters

| Param | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `group_id` | uint | ✓ |  | 群号 |
| `chat_type` | int | – | `1` |  |

## Returns (data)

按分类分组的 AI 语音角色列表，每组含分类名与角色（id、名称、试听 URL）。

Every response is wrapped in the standard envelope: `{ "status": "ok", "retcode": 0, "data": ... }`. The table below describes `data`.

`data` is an array; each element:

| Field | Type | Required | Description |
| --- | --- | --- | --- |
| `type` | string | ✓ | 角色分类名 |
| `characters` | object[] | ✓ | 该分类下的角色列表 |

<details>
<summary>Raw JSON Schema</summary>

```json
{
  "type": "array",
  "items": {
    "type": "object",
    "properties": {
      "type": {
        "type": "string",
        "description": "角色分类名"
      },
      "characters": {
        "type": "array",
        "description": "该分类下的角色列表",
        "items": {
          "type": "object",
          "properties": {
            "character_id": {
              "type": "string",
              "description": "角色 ID"
            },
            "character_name": {
              "type": "string",
              "description": "角色显示名"
            },
            "preview_url": {
              "type": "string",
              "description": "试听音频 URL"
            }
          },
          "required": [
            "character_id",
            "character_name",
            "preview_url"
          ]
        }
      }
    },
    "required": [
      "type",
      "characters"
    ]
  }
}
```

</details>

## Examples

<Tabs>
<Tab label="curl">

```bash
curl -X POST http://127.0.0.1:3000/get_ai_characters \
  -H 'Content-Type: application/json' \
  -H 'Authorization: Bearer <access-token>' \
  -d '{"group_id":123456,"chat_type":1}'
```

</Tab>
<Tab label="Python">

```python
import requests

resp = requests.post(
    "http://127.0.0.1:3000/get_ai_characters",
    json={
        "group_id": 123456,
        "chat_type": 1
    },
    headers={"Authorization": "Bearer <access-token>"},  # if an access token is configured
)
print(resp.json())
```

</Tab>
<Tab label="JavaScript">

```js
const resp = await fetch("http://127.0.0.1:3000/get_ai_characters", {
  method: "POST",
  headers: {
    "Content-Type": "application/json",
    "Authorization": "Bearer <access-token>", // if an access token is configured
  },
  body: JSON.stringify({
  "group_id": 123456,
  "chat_type": 1
}),
});
console.log(await resp.json());
```

</Tab>
<Tab label="Go">

```go
package main

import (
	"bytes"
	"fmt"
	"io"
	"net/http"
)

func main() {
	body := []byte(`{"group_id":123456,"chat_type":1}`)
	req, _ := http.NewRequest("POST", "http://127.0.0.1:3000/get_ai_characters", bytes.NewReader(body))
	req.Header.Set("Content-Type", "application/json")
	req.Header.Set("Authorization", "Bearer <access-token>") // if an access token is configured
	resp, err := http.DefaultClient.Do(req)
	if err != nil {
		panic(err)
	}
	defer resp.Body.Close()
	out, _ := io.ReadAll(resp.Body)
	fmt.Println(string(out))
}
```

</Tab>
<Tab label="SnowLuma SDK">

```ts
import { SnowLumaHttpClient } from '@snowluma/sdk';

const bot = new SnowLumaHttpClient({
  baseUrl: 'http://127.0.0.1:3000/',
  accessToken: process.env.SNOWLUMA_TOKEN, // if an access token is configured
});

const data = await bot.call('get_ai_characters', {
  "group_id": 123456,
  "chat_type": 1
});
console.log(data);
```

</Tab>
</Tabs>

:::tip Using an AI assistant?
MCP-capable clients can discover and call this action directly — no hand-written HTTP. See [MCP](/en/mcp/).
:::
