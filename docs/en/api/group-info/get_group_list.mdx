---
title: "get_group_list"
---

import { ActionHeader } from '../../../components/ActionHeader';
import { Tabs, Tab } from '@theme';

<ActionHeader name={"get_group_list"} summary={"获取群列表"} readOnly={true} category={"群信息"} />

Call via `POST http://127.0.0.1:3000/get_group_list` (JSON params as the body) or WebSocket.

## Parameters

| Param | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `no_cache` | bool | – | `false` |  |

## Returns (data)

群信息对象数组。

Every response is wrapped in the standard envelope: `{ "status": "ok", "retcode": 0, "data": ... }`. The table below describes `data`.

`data` is an array; each element:

| Field | Type | Required | Description |
| --- | --- | --- | --- |
| `group_id` | integer | ✓ | 群号 |
| `group_name` | string | ✓ | 群名 |
| `member_count` | integer | ✓ | 当前成员数 |
| `max_member_count` | integer | ✓ | 成员上限 |

<details>
<summary>Raw JSON Schema</summary>

```json
{
  "type": "array",
  "items": {
    "type": "object",
    "properties": {
      "group_id": {
        "type": "integer",
        "description": "群号"
      },
      "group_name": {
        "type": "string",
        "description": "群名"
      },
      "member_count": {
        "type": "integer",
        "description": "当前成员数"
      },
      "max_member_count": {
        "type": "integer",
        "description": "成员上限"
      }
    },
    "required": [
      "group_id",
      "group_name",
      "member_count",
      "max_member_count"
    ]
  }
}
```

</details>

## Examples

<Tabs>
<Tab label="curl">

```bash
curl -X POST http://127.0.0.1:3000/get_group_list \
  -H 'Content-Type: application/json' \
  -H 'Authorization: Bearer <access-token>' \
  -d '{"no_cache":false}'
```

</Tab>
<Tab label="Python">

```python
import requests

resp = requests.post(
    "http://127.0.0.1:3000/get_group_list",
    json={
        "no_cache": False
    },
    headers={"Authorization": "Bearer <access-token>"},  # if an access token is configured
)
print(resp.json())
```

</Tab>
<Tab label="JavaScript">

```js
const resp = await fetch("http://127.0.0.1:3000/get_group_list", {
  method: "POST",
  headers: {
    "Content-Type": "application/json",
    "Authorization": "Bearer <access-token>", // if an access token is configured
  },
  body: JSON.stringify({
  "no_cache": false
}),
});
console.log(await resp.json());
```

</Tab>
<Tab label="Go">

```go
package main

import (
	"bytes"
	"fmt"
	"io"
	"net/http"
)

func main() {
	body := []byte(`{"no_cache":false}`)
	req, _ := http.NewRequest("POST", "http://127.0.0.1:3000/get_group_list", bytes.NewReader(body))
	req.Header.Set("Content-Type", "application/json")
	req.Header.Set("Authorization", "Bearer <access-token>") // if an access token is configured
	resp, err := http.DefaultClient.Do(req)
	if err != nil {
		panic(err)
	}
	defer resp.Body.Close()
	out, _ := io.ReadAll(resp.Body)
	fmt.Println(string(out))
}
```

</Tab>
<Tab label="SnowLuma SDK">

```ts
import { SnowLumaHttpClient } from '@snowluma/sdk';

const bot = new SnowLumaHttpClient({
  baseUrl: 'http://127.0.0.1:3000/',
  accessToken: process.env.SNOWLUMA_TOKEN, // if an access token is configured
});

const data = await bot.call('get_group_list', {
  "no_cache": false
});
console.log(data);
```

</Tab>
</Tabs>

:::tip Using an AI assistant?
MCP-capable clients can discover and call this action directly — no hand-written HTTP. See [MCP](/en/mcp/).
:::
