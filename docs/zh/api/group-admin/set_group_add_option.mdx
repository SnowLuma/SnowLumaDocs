---
title: "set_group_add_option"
---

import { ActionHeader } from '../../../components/ActionHeader';
import { Tabs, Tab } from '@theme';

<ActionHeader name={"set_group_add_option"} summary={"设置加群选项"} readOnly={false} category={"群管理"} />

通过 `POST http://127.0.0.1:3000/set_group_add_option`（请求体为 JSON 参数）或 WebSocket 调用。

## 输入参数

| 参数 | 类型 | 必填 | 默认 | 说明 |
| --- | --- | --- | --- | --- |
| `group_id` | uint | ✓ |  | 群号 |
| `add_type` | int | – | `0` |  |

## 返回 (data)

该动作返回标准 OneBot 信封；`data` 结构待补充。

## 调用示例

<Tabs>
<Tab label="curl">

```bash
curl -X POST http://127.0.0.1:3000/set_group_add_option \
  -H 'Content-Type: application/json' \
  -H 'Authorization: Bearer <access-token>' \
  -d '{"group_id":123456,"add_type":0}'
```

</Tab>
<Tab label="Python">

```python
import requests

resp = requests.post(
    "http://127.0.0.1:3000/set_group_add_option",
    json={
        "group_id": 123456,
        "add_type": 0
    },
    headers={"Authorization": "Bearer <access-token>"},  # 若已配置 access token
)
print(resp.json())
```

</Tab>
<Tab label="JavaScript">

```js
const resp = await fetch("http://127.0.0.1:3000/set_group_add_option", {
  method: "POST",
  headers: {
    "Content-Type": "application/json",
    "Authorization": "Bearer <access-token>", // 若已配置 access token
  },
  body: JSON.stringify({
  "group_id": 123456,
  "add_type": 0
}),
});
console.log(await resp.json());
```

</Tab>
<Tab label="Go">

```go
package main

import (
	"bytes"
	"fmt"
	"io"
	"net/http"
)

func main() {
	body := []byte(`{"group_id":123456,"add_type":0}`)
	req, _ := http.NewRequest("POST", "http://127.0.0.1:3000/set_group_add_option", bytes.NewReader(body))
	req.Header.Set("Content-Type", "application/json")
	req.Header.Set("Authorization", "Bearer <access-token>") // 若已配置 access token
	resp, err := http.DefaultClient.Do(req)
	if err != nil {
		panic(err)
	}
	defer resp.Body.Close()
	out, _ := io.ReadAll(resp.Body)
	fmt.Println(string(out))
}
```

</Tab>
<Tab label="SnowLuma SDK">

```ts
import { SnowLumaHttpClient } from '@snowluma/sdk';

const bot = new SnowLumaHttpClient({
  baseUrl: 'http://127.0.0.1:3000/',
  accessToken: process.env.SNOWLUMA_TOKEN, // 若已配置 access token
});

const data = await bot.call('set_group_add_option', {
  "group_id": 123456,
  "add_type": 0
});
console.log(data);
```

</Tab>
</Tabs>

:::tip 在用 AI 助手？
支持 MCP 的客户端可直接发现并调用本动作，无需手写 HTTP。见 [MCP 接入](/mcp/)。
:::
