---
title: "del_group_album_media"
---

import { ActionHeader } from '../../../components/ActionHeader';
import { Tabs, Tab } from '@theme';

<ActionHeader name={"del_group_album_media"} readOnly={false} category={"群相册"} />

通过 `POST http://127.0.0.1:3000/del_group_album_media`（请求体为 JSON 参数）或 WebSocket 调用。

## 输入参数

| 参数 | 类型 | 必填 | 默认 | 说明 |
| --- | --- | --- | --- | --- |
| `group_id` | uint | ✓ |  | 群号 |
| `album_id` | string | ✓ |  |  |
| `lloc` | string | ✓ |  |  |

## 返回 (data)

该动作返回标准 OneBot 信封；`data` 结构待补充。

## 调用示例

<Tabs>
<Tab label="curl">

```bash
curl -X POST http://127.0.0.1:3000/del_group_album_media \
  -H 'Content-Type: application/json' \
  -H 'Authorization: Bearer <access-token>' \
  -d '{"group_id":123456,"album_id":"","lloc":""}'
```

</Tab>
<Tab label="Python">

```python
import requests

resp = requests.post(
    "http://127.0.0.1:3000/del_group_album_media",
    json={
        "group_id": 123456,
        "album_id": "",
        "lloc": ""
    },
    headers={"Authorization": "Bearer <access-token>"},  # 若已配置 access token
)
print(resp.json())
```

</Tab>
<Tab label="JavaScript">

```js
const resp = await fetch("http://127.0.0.1:3000/del_group_album_media", {
  method: "POST",
  headers: {
    "Content-Type": "application/json",
    "Authorization": "Bearer <access-token>", // 若已配置 access token
  },
  body: JSON.stringify({
  "group_id": 123456,
  "album_id": "",
  "lloc": ""
}),
});
console.log(await resp.json());
```

</Tab>
<Tab label="Go">

```go
package main

import (
	"bytes"
	"fmt"
	"io"
	"net/http"
)

func main() {
	body := []byte(`{"group_id":123456,"album_id":"","lloc":""}`)
	req, _ := http.NewRequest("POST", "http://127.0.0.1:3000/del_group_album_media", bytes.NewReader(body))
	req.Header.Set("Content-Type", "application/json")
	req.Header.Set("Authorization", "Bearer <access-token>") // 若已配置 access token
	resp, err := http.DefaultClient.Do(req)
	if err != nil {
		panic(err)
	}
	defer resp.Body.Close()
	out, _ := io.ReadAll(resp.Body)
	fmt.Println(string(out))
}
```

</Tab>
<Tab label="SnowLuma SDK">

```ts
import { SnowLumaHttpClient } from '@snowluma/sdk';

const bot = new SnowLumaHttpClient({
  baseUrl: 'http://127.0.0.1:3000/',
  accessToken: process.env.SNOWLUMA_TOKEN, // 若已配置 access token
});

const data = await bot.call('del_group_album_media', {
  "group_id": 123456,
  "album_id": "",
  "lloc": ""
});
console.log(data);
```

</Tab>
</Tabs>

:::tip 在用 AI 助手？
支持 MCP 的客户端可直接发现并调用本动作，无需手写 HTTP。见 [MCP 接入](/mcp/)。
:::
