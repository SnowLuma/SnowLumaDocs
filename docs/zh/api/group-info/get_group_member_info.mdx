---
title: "get_group_member_info"
---

import { ActionHeader } from '../../../components/ActionHeader';
import { Tabs, Tab } from '@theme';

<ActionHeader name={"get_group_member_info"} summary={"获取群成员信息"} readOnly={true} category={"群信息"} />

通过 `POST http://127.0.0.1:3000/get_group_member_info`（请求体为 JSON 参数）或 WebSocket 调用。

## 输入参数

| 参数 | 类型 | 必填 | 默认 | 说明 |
| --- | --- | --- | --- | --- |
| `group_id` | uint | ✓ |  | 群号 |
| `user_id` | uint | ✓ |  | QQ 号 |
| `no_cache` | bool | – | `false` |  |

## 返回 (data)

群成员信息对象。

所有响应都包裹在标准信封中：`{ "status": "ok", "retcode": 0, "data": ... }`。下表描述 `data` 字段。

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| `group_id` | integer | ✓ | 群号 |
| `user_id` | integer | ✓ | QQ 号 |
| `nickname` | string | ✓ | 昵称 |
| `card` | string | – | 群名片 |
| `sex` | string (male / female / unknown) | – | 性别 |
| `age` | integer | – | 年龄 |
| `join_time` | integer | – | 入群时间戳（秒） |
| `last_sent_time` | integer | – | 最后发言时间戳（秒） |
| `level` | string | – | 群等级 |
| `role` | string (owner / admin / member) | ✓ | 角色 |
| `title` | string | – | 专属头衔 |

<details>
<summary>原始 JSON Schema</summary>

```json
{
  "type": "object",
  "properties": {
    "group_id": {
      "type": "integer",
      "description": "群号"
    },
    "user_id": {
      "type": "integer",
      "description": "QQ 号"
    },
    "nickname": {
      "type": "string",
      "description": "昵称"
    },
    "card": {
      "type": "string",
      "description": "群名片"
    },
    "sex": {
      "type": "string",
      "enum": [
        "male",
        "female",
        "unknown"
      ],
      "description": "性别"
    },
    "age": {
      "type": "integer",
      "description": "年龄"
    },
    "join_time": {
      "type": "integer",
      "description": "入群时间戳（秒）"
    },
    "last_sent_time": {
      "type": "integer",
      "description": "最后发言时间戳（秒）"
    },
    "level": {
      "type": "string",
      "description": "群等级"
    },
    "role": {
      "type": "string",
      "enum": [
        "owner",
        "admin",
        "member"
      ],
      "description": "角色"
    },
    "title": {
      "type": "string",
      "description": "专属头衔"
    }
  },
  "required": [
    "group_id",
    "user_id",
    "nickname",
    "role"
  ]
}
```

</details>

## 调用示例

<Tabs>
<Tab label="curl">

```bash
curl -X POST http://127.0.0.1:3000/get_group_member_info \
  -H 'Content-Type: application/json' \
  -H 'Authorization: Bearer <access-token>' \
  -d '{"group_id":123456,"user_id":10001,"no_cache":false}'
```

</Tab>
<Tab label="Python">

```python
import requests

resp = requests.post(
    "http://127.0.0.1:3000/get_group_member_info",
    json={
        "group_id": 123456,
        "user_id": 10001,
        "no_cache": False
    },
    headers={"Authorization": "Bearer <access-token>"},  # 若已配置 access token
)
print(resp.json())
```

</Tab>
<Tab label="JavaScript">

```js
const resp = await fetch("http://127.0.0.1:3000/get_group_member_info", {
  method: "POST",
  headers: {
    "Content-Type": "application/json",
    "Authorization": "Bearer <access-token>", // 若已配置 access token
  },
  body: JSON.stringify({
  "group_id": 123456,
  "user_id": 10001,
  "no_cache": false
}),
});
console.log(await resp.json());
```

</Tab>
<Tab label="Go">

```go
package main

import (
	"bytes"
	"fmt"
	"io"
	"net/http"
)

func main() {
	body := []byte(`{"group_id":123456,"user_id":10001,"no_cache":false}`)
	req, _ := http.NewRequest("POST", "http://127.0.0.1:3000/get_group_member_info", bytes.NewReader(body))
	req.Header.Set("Content-Type", "application/json")
	req.Header.Set("Authorization", "Bearer <access-token>") // 若已配置 access token
	resp, err := http.DefaultClient.Do(req)
	if err != nil {
		panic(err)
	}
	defer resp.Body.Close()
	out, _ := io.ReadAll(resp.Body)
	fmt.Println(string(out))
}
```

</Tab>
<Tab label="SnowLuma SDK">

```ts
import { SnowLumaHttpClient } from '@snowluma/sdk';

const bot = new SnowLumaHttpClient({
  baseUrl: 'http://127.0.0.1:3000/',
  accessToken: process.env.SNOWLUMA_TOKEN, // 若已配置 access token
});

const data = await bot.call('get_group_member_info', {
  "group_id": 123456,
  "user_id": 10001,
  "no_cache": false
});
console.log(data);
```

</Tab>
</Tabs>

:::tip 在用 AI 助手？
支持 MCP 的客户端可直接发现并调用本动作，无需手写 HTTP。见 [MCP 接入](/mcp/)。
:::
