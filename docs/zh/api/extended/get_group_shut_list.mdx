---
title: "get_group_shut_list"
---

import { ActionHeader } from '../../../components/ActionHeader';
import { Tabs, Tab } from '@theme';

<ActionHeader name={"get_group_shut_list"} summary={"获取群禁言列表"} readOnly={true} category={"扩展"} />

通过 `POST http://127.0.0.1:3000/get_group_shut_list`（请求体为 JSON 参数）或 WebSocket 调用。

## 输入参数

| 参数 | 类型 | 必填 | 默认 | 说明 |
| --- | --- | --- | --- | --- |
| `group_id` | uint | ✓ |  | 群号 |

## 返回 (data)

仍在禁言中的成员数组，每项含 QQ 号、昵称与禁言到期时间戳（秒）。

所有响应都包裹在标准信封中：`{ "status": "ok", "retcode": 0, "data": ... }`。下表描述 `data` 字段。

`data` 为数组，每个元素：

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| `user_id` | integer | ✓ | 成员 QQ 号 |
| `nickname` | string | ✓ | 成员昵称 |
| `shut_up_time` | integer | ✓ | 禁言到期时间戳（秒） |

<details>
<summary>原始 JSON Schema</summary>

```json
{
  "type": "array",
  "items": {
    "type": "object",
    "properties": {
      "user_id": {
        "type": "integer",
        "description": "成员 QQ 号"
      },
      "nickname": {
        "type": "string",
        "description": "成员昵称"
      },
      "shut_up_time": {
        "type": "integer",
        "description": "禁言到期时间戳（秒）"
      }
    },
    "required": [
      "user_id",
      "nickname",
      "shut_up_time"
    ]
  }
}
```

</details>

## 调用示例

<Tabs>
<Tab label="curl">

```bash
curl -X POST http://127.0.0.1:3000/get_group_shut_list \
  -H 'Content-Type: application/json' \
  -H 'Authorization: Bearer <access-token>' \
  -d '{"group_id":123456}'
```

</Tab>
<Tab label="Python">

```python
import requests

resp = requests.post(
    "http://127.0.0.1:3000/get_group_shut_list",
    json={
        "group_id": 123456
    },
    headers={"Authorization": "Bearer <access-token>"},  # 若已配置 access token
)
print(resp.json())
```

</Tab>
<Tab label="JavaScript">

```js
const resp = await fetch("http://127.0.0.1:3000/get_group_shut_list", {
  method: "POST",
  headers: {
    "Content-Type": "application/json",
    "Authorization": "Bearer <access-token>", // 若已配置 access token
  },
  body: JSON.stringify({
  "group_id": 123456
}),
});
console.log(await resp.json());
```

</Tab>
<Tab label="Go">

```go
package main

import (
	"bytes"
	"fmt"
	"io"
	"net/http"
)

func main() {
	body := []byte(`{"group_id":123456}`)
	req, _ := http.NewRequest("POST", "http://127.0.0.1:3000/get_group_shut_list", bytes.NewReader(body))
	req.Header.Set("Content-Type", "application/json")
	req.Header.Set("Authorization", "Bearer <access-token>") // 若已配置 access token
	resp, err := http.DefaultClient.Do(req)
	if err != nil {
		panic(err)
	}
	defer resp.Body.Close()
	out, _ := io.ReadAll(resp.Body)
	fmt.Println(string(out))
}
```

</Tab>
<Tab label="SnowLuma SDK">

```ts
import { SnowLumaHttpClient } from '@snowluma/sdk';

const bot = new SnowLumaHttpClient({
  baseUrl: 'http://127.0.0.1:3000/',
  accessToken: process.env.SNOWLUMA_TOKEN, // 若已配置 access token
});

const data = await bot.call('get_group_shut_list', {
  "group_id": 123456
});
console.log(data);
```

</Tab>
</Tabs>

:::tip 在用 AI 助手？
支持 MCP 的客户端可直接发现并调用本动作，无需手写 HTTP。见 [MCP 接入](/mcp/)。
:::
