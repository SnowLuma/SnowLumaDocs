---
title: "get_credentials"
---

import { ActionHeader } from '../../../components/ActionHeader';
import { Tabs, Tab } from '@theme';

<ActionHeader name={"get_credentials"} summary={"获取凭证"} readOnly={true} category={"扩展"} />

通过 `POST http://127.0.0.1:3000/get_credentials`（请求体为 JSON 参数）或 WebSocket 调用。

## 输入参数

| 参数 | 类型 | 必填 | 默认 | 说明 |
| --- | --- | --- | --- | --- |
| `domain` | string | – | `"qun.qq.com"` |  |

## 返回 (data)

{ cookies, token, csrf_token }：Cookie 字符串与 CSRF 令牌（token 与 csrf_token 同值）。

所有响应都包裹在标准信封中：`{ "status": "ok", "retcode": 0, "data": ... }`。下表描述 `data` 字段。

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| `cookies` | string | ✓ | 该域名的 Cookie 字符串 |
| `token` | integer | ✓ | CSRF 令牌（bkn） |
| `csrf_token` | integer | ✓ | CSRF 令牌（同 token） |

<details>
<summary>原始 JSON Schema</summary>

```json
{
  "type": "object",
  "properties": {
    "cookies": {
      "type": "string",
      "description": "该域名的 Cookie 字符串"
    },
    "token": {
      "type": "integer",
      "description": "CSRF 令牌（bkn）"
    },
    "csrf_token": {
      "type": "integer",
      "description": "CSRF 令牌（同 token）"
    }
  },
  "required": [
    "cookies",
    "token",
    "csrf_token"
  ]
}
```

</details>

## 调用示例

<Tabs>
<Tab label="curl">

```bash
curl -X POST http://127.0.0.1:3000/get_credentials \
  -H 'Content-Type: application/json' \
  -H 'Authorization: Bearer <access-token>' \
  -d '{"domain":"qun.qq.com"}'
```

</Tab>
<Tab label="Python">

```python
import requests

resp = requests.post(
    "http://127.0.0.1:3000/get_credentials",
    json={
        "domain": "qun.qq.com"
    },
    headers={"Authorization": "Bearer <access-token>"},  # 若已配置 access token
)
print(resp.json())
```

</Tab>
<Tab label="JavaScript">

```js
const resp = await fetch("http://127.0.0.1:3000/get_credentials", {
  method: "POST",
  headers: {
    "Content-Type": "application/json",
    "Authorization": "Bearer <access-token>", // 若已配置 access token
  },
  body: JSON.stringify({
  "domain": "qun.qq.com"
}),
});
console.log(await resp.json());
```

</Tab>
<Tab label="Go">

```go
package main

import (
	"bytes"
	"fmt"
	"io"
	"net/http"
)

func main() {
	body := []byte(`{"domain":"qun.qq.com"}`)
	req, _ := http.NewRequest("POST", "http://127.0.0.1:3000/get_credentials", bytes.NewReader(body))
	req.Header.Set("Content-Type", "application/json")
	req.Header.Set("Authorization", "Bearer <access-token>") // 若已配置 access token
	resp, err := http.DefaultClient.Do(req)
	if err != nil {
		panic(err)
	}
	defer resp.Body.Close()
	out, _ := io.ReadAll(resp.Body)
	fmt.Println(string(out))
}
```

</Tab>
<Tab label="SnowLuma SDK">

```ts
import { SnowLumaHttpClient } from '@snowluma/sdk';

const bot = new SnowLumaHttpClient({
  baseUrl: 'http://127.0.0.1:3000/',
  accessToken: process.env.SNOWLUMA_TOKEN, // 若已配置 access token
});

const data = await bot.call('get_credentials', {
  "domain": "qun.qq.com"
});
console.log(data);
```

</Tab>
</Tabs>

:::tip 在用 AI 助手？
支持 MCP 的客户端可直接发现并调用本动作，无需手写 HTTP。见 [MCP 接入](/mcp/)。
:::
